#!/usr/bin/env python3
"""Placeholder blocked audit.

The previous heuristic Teacher reconstruction was not source-bound to the frozen
R10.2/R10.3 privileged Teacher contract and therefore must not be used to claim
Student deployment recall or FSM opportunity failure.

A replacement audit must import or exactly reproduce the sealed Teacher labeler,
verify input sufficiency from the privileged sidecar, and replay the runtime FSM
from recorded close_mask / anchor / state fields rather than re-derive a second
approximate scheduler.
"""

raise SystemExit(
    "HOLD_HEURISTIC_TEACHER_AUDIT_SUPERSEDED: use frozen R10.2/R10.3 Teacher contract"
)
