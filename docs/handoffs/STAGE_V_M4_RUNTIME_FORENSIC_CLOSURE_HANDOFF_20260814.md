# Stage V M4 Runtime Forensic Closure Handoff

**Date:** 2026-08-14
**Purpose:** handoff for an independent GPT/Codex review and generation of the next self-contained Goal Mode prompt.
**Status:** review-only; formal M4 remains on HOLD.

## 1. Executive decision

The current project is not authorized to read new held-out causal outcomes. The correct state is:

```text
Teacher/Student freeze: PASS and sealed
pre-M4 lock: PASS and sealed
exact 40x24 plan: immutable selection authority
formal runtime V2: historical authorization is not reusable
GLOBAL_HOLD: active
Parent 00: HOLD_SEALED after independent structural invalidation
Parent 01: HOLD_FORMAL_M4_STRUCTURAL_FAILURE, closed before first counterfactual branch
V_phys: not generated
protected evaluation / attack / visual robustness / Eval160: not read
```

The next legal work is a CPU/read-only authority repair and compatibility audit. It must not become a new scientific experiment. No treatment branch, causal label producer, or protected read may be executed until a new successor authority is independently sealed.

The handoff asks GPT to review the evidence below and return a new, self-contained Goal Mode prompt that another Codex window can execute without relying on chat history.

## 2. Immutable scientific boundaries

These boundaries are authoritative and must not be weakened by the next window:

1. The primary ordering is clean data -> privileged clean Teacher -> causal deployment-facing Student -> Teacher/Student freeze -> pre-M4 lock -> held-out M4.
2. M4 is held-out causal validation, not a supervision source. M4 outcomes must not influence Teacher/Student features, labels, model selection, thresholds, stopping rules, or emit logic.
3. The historical M4 -> V-Teacher -> Student plan is superseded historical planning only. It must not be revived.
4. The current Student contract is four active heads only:
   - `physical_criticality`
   - `k10_feasibility`
   - `instability`
   - `gripper_closing_state`
5. `safe_release` remains `HOLD_COVERAGE` and contributes zero training gradient.
6. Student inputs remain deployment-facing causal 25D features. No privileged Teacher field, future field, M4 outcome, protected outcome, identity shortcut, or post-treatment variable may enter the Student input contract.
7. Validation is the only threshold/model-selection surface. The G7 test path was read once and is already frozen; test/M4 outcomes must not be used to revise the detector.
8. V2 terminal HOLD is immutable. Failed parents must not be rerun until they pass.
9. Exact probe IDs and steps are frozen. Any runtime that recomputes or silently reseats probes fails closed.
10. GPU availability is an execution resource only. It is not a scientific qualification result, a sample-size claim, or permission to bypass an authority gate.

## 3. Repository and GitHub state at takeover

Repository:

```text
local worktree: D:\vla_attack\repo_work\openvla-gripper-dutycycle-attack-post-32-of-40-hold-20260813
branch: codex/m4-corridor-replenishment-post-32-of-40-hold-20260813
PR: https://github.com/Leo-6-maker/openvla-gripper-dutycycle-attack/pull/112
base: codex/stage-v-resource-contract-20260810
```

The handoff is append-only. It is a documentation/review artifact; it does not authorize runtime execution. The branch already contains the minimal governance and closure-audit changes described in Section 8. The final commit SHA and tree should be re-read from GitHub after push rather than inferred from this document.

At the last verified pre-handoff state:

```text
local HEAD: e6b779b38a8f3a044d2925a1a0ea441416d5c545
local tree: 5174f552b89cb9f886f946d5213fdb7fca977607
PR state: OPEN / DRAFT / MERGEABLE
checks at e6b779b3: source-registry PASS, detector-v5-cpu PASS, stageb-cpu PASS
local detector_v5 suite: 326 passed, 6 skipped
```

These values are a takeover snapshot. The receiving window must verify the live PR head, tree, checks, and worktree status before making any further change.

## 4. Server and runtime roles

Server:

```text
SSH alias: dty-server
host: 10.60.133.3
official Python: /mnt/sdc/dty_user/openvla_attack/envs/openvla-official-a800/bin/python
formal worktree: /mnt/sdc/dty_user/openvla_attack_worktrees/stage-v-primary-3370d8cf
```

The live formal worktree is clean at `8951731c34595bcc73295f3c9c7390b13a219154`. The closure-audit tools from the current branch were copied to the separate path below so the historical formal source plane was not modified:

```text
/mnt/sdc/dty_user/stage_v_closure_audit_e6b779b3/
```

At the last live check there were no formal M4 workers or processes. The GPUs were occupied by external workloads and must not be stopped or reused without a fresh ownership check. The observed free memory/utilization snapshot was:

```text
GPU0 1582 MiB / 100%
GPU1 1582 MiB / 98%
GPU2 1582 MiB / 93%
GPU3 1582 MiB / 94%
GPU4 1582 MiB / 95%
GPU5 1666 MiB / 95%
GPU6 64098 MiB / 46%
GPU7 1582 MiB / 95%
```

This is diagnostic state only. It is not permission to attach to GPU6, and no foreign process was changed. Any future successor canary requires a fresh `nvidia-smi --query-compute-apps` ownership check and the strict lower bound `free_memory_mib > 20480`.

## 5. Teacher/Student authority already sealed

The primary Teacher/Student line is complete enough to seal and has not consumed M4 outcomes:

```text
Teacher labels: 670 parents, 196,483 clean records
label source: privileged clean physical-criticality labels from FIT670 clean executions
active Student heads: 4 listed in Section 2
safe_release: HOLD_COVERAGE, zero gradient
features: deployment-facing causal 25D
G7 test read: PASS_R3_G7_TEST_EVALUATION, test_read_count=1
threshold timing: frozen before test read
M4/protected outcome reads: none
```

Sealed authority artifacts:

```text
Teacher/Student freeze report SHA: 2e0557f580cbdb334b6cf56725600e08b3e7b58301df4031e3f671047fe1eb69
pre-M4 lock report SHA: 9b5741cbd9a52813dd04e8aaa2f76b4bbb96276960107e6d56781805cabdddd9
V1 supersession receipt SHA: dc46325c0d26ead32587203f599121be2d3c453e0eba4f74be8f25f118f77f6f
```

The historical N5/D8 checkpoints, thresholds, and development artifacts remain non-consumable unless explicitly promoted by the current frozen protocol. The current freeze must be treated as an input to the successor M4 authority, not as permission to read M4 outcomes.

## 6. Exact plan and source-plane provenance

Frozen exact-plan root:

```text
/mnt/sdc/dty_user/openvla_attack_outputs/n5/phase3_student/STAGE_V_M4_EXACT_40X24_PLAN_ONLY_V1_20260813T064532Z
audit SHA: cc7b01f45e77c4a2907a630bc0917dda8240ddc551beda34134fffc46eff443d
manifest SHA: 0c6caa4dd6638a5d06dfb5f36cc3a7270000b988b3e16e97e7008dd017a3fdf0
```

The 40 parents x 24 probes authority is immutable. The formal science matrix remains 960 probes x 4 branches, or 3,840 physical branch executions, subject to all current authorization gates.

Important source roles:

```text
snapshot/exact-plan plane commit: 3bb52975f2f48bb8250d02aa00d7f2fbcc633a65
snapshot/exact-plan plane tree:   7d9870d4897a6314fdb8169ea7d0b5d998ec73b4
formal runtime plane commit:     3b1c6ed1255cf6dbd76ac63bf0617579cd5b55eb
formal runtime plane tree:       45458f269a9b9aa57157fe9b2ac715a3e2e5754b
live formal worktree commit:     8951731c34595bcc73295f3c9c7390b13a219154
```

The following core blobs were reviewed as unchanged across the relevant planes:

```text
snapshot capture/restore blob: 3b618e66f5988d1cad8e77fe458e14c20e170009
Gate-A blob:                   2b256248aaf3e224c7c839d63d78ae20c9afdb4a
Gate-B blob:                   64ed1ec477aab09f49b436b48cf4e8eae830aa75
```

The old nested `downstream_source` in the exact-plan artifact points to the snapshot plane. The current governance guard correctly refuses to consume it as if it were the formal runtime source. This is an authority mismatch, not a reason to regenerate probes or rewrite the frozen plan.

## 7. Formal root and immutable parent outcomes

Formal root:

```text
/mnt/sdc/dty_user/openvla_attack_outputs/n5/phase3_student/STAGE_V_M4_FORMAL_RUNTIME_V2_GPU6_19G_PRELAUNCH_20260813T154500Z
GLOBAL_HOLD.json SHA: f5e3b37db9c438e0c9219d56b8bf92e7d02d6e8c40c69f03bc6986c9bc203db5
global status: HOLD_STOP_GLOBAL_SCHEDULING
```

Parent 00:

```text
parents/00_libero_10__task_01__state_42
PARENT_RESULT: HOLD_SEALED
M4_INDEPENDENT_AUDIT: FAIL_M4_PARENT_INDEPENDENT
probes: 24
branch records: 96
label containers: 72
protected counters: zero
```

Parent 00 must remain immutable. Its failure is not to be converted into a successful scientific result by rerunning it.

Parent 01:

```text
parents/01_libero_10__task_06__state_30
PARENT_STATUS: HOLD_FORMAL_M4_STRUCTURAL_FAILURE
counterfactual branches started: no
branch/label/parent-result artifacts: absent
lease release: PASS
outcomes_read_uncertain: true, preserved conservatively
```

Parent 01 is a structural pre-branch closure, not a physical result.

## 8. Minimal code changes already made

The branch contains only the smallest changes needed to make the authority defect auditable and to close the two observed parent attempts:

### 8.1 Exact-plan source binding guard

Commit `8951731c` adds a governance check requiring the exact manifest `downstream_source.commit/tree` to match the formal runtime source. A mismatch raises:

```text
M4_V2_EXACT_PLAN_SOURCE_BINDING_INVALID
```

This prevents a snapshot-plane manifest from being silently consumed by a different formal runtime source.

### 8.2 Parent 00 read-only pre-intervention closure audit

The auditor reads the immutable Parent 00 records and reports branch-level evidence without generating or editing M4 labels. It now records the Gate-B runner and auditor provenance, classifies the failure stage, and distinguishes the clean-prefix replay from the post-snapshot primary window.

### 8.3 Parent 01 read-only pre-branch abort closure audit

The auditor verifies `PARENT_STATUS.json`, lease/resource release, absence of branch/label/result artifacts, and protected counters. It does not reopen the parent or execute a branch.

Relevant commits:

```text
8951731c stage-v: bind exact plan to formal source plane
56880c2a stage-v: audit pre-intervention parent closure
808c0984 stage-v: bind structural closure to gate-b stage
f820bf92 stage-v: record closure auditor provenance
e6b779b3 stage-v: scope closure steps to primary window
```

Focused closure tests pass; the local full detector-v5 suite is `326 passed, 6 skipped`.

## 9. Independent closure evidence

### 9.1 Parent 00 closure root

```text
root: /mnt/sdc/dty_user/openvla_attack_outputs/n5/phase3_student/STAGE_V_M4_PARENT00_PREINTERVENTION_CLOSURE_AUDIT_E6B779B3_20260813T165313Z
report: PREINTERVENTION_STRUCTURAL_INVALIDATION_AUDIT.json
report SHA: adfec5855839c80a6a38042573567dc4ee4c041f6294a00e8dc0cb6c9bd29242
SHA256SUMS SHA: f6d2f7453f104d10a24c5374e62734110f0a1c760981ed0f2579f23484af2756
SHA256SUMS.sha256 SHA: 5731aec907800056632166d015fd2e5e70467dd1d11901a2d8f6acfe9eb7db0f
```

Verified result:

```text
status=PASS_PREINTERVENTION_STRUCTURAL_INVALIDATION
branch_record_count=96
pass_branch_count=0
fail_branch_count=96
failure_stage_counts={"PRE_PRIMARY_WINDOW_RUNTIME_EXACTNESS":96}
pre_primary_restore_failure_count=96
post_snapshot_primary_window_steps_total=0
control_primary_window_steps_total=0
treatment_primary_window_steps_total=0
forced_open_steps_total=0
treatment_receipts_total=0
binary_label_consumable_count=0
valid_v_phys_count=0
protected counters=0
clean-prefix replay=explicitly excluded from the claim
```

Interpretation: every Parent 00 branch failed before the primary window because the restored runtime state did not satisfy exact binding. There is no evidence of a post-snapshot primary-window step, treatment receipt, binary consumable label, or physical outcome.

### 9.2 Parent 01 closure root

```text
root: /mnt/sdc/dty_user/openvla_attack_outputs/n5/phase3_student/STAGE_V_M4_PARENT01_PREBRANCH_ABORT_CLOSURE_AUDIT_E6B779B3_20260813T165313Z
report: PREBRANCH_ABORT_CLOSURE.json
report SHA: 48e51bf359e146036f83aa12e16880e76af673eca7babbcf39f0a0831c21185c
SHA256SUMS SHA: 68236087f20337d1e259907d91b7d60a4b11a97a43d1c005c8632f1370ce116f
SHA256SUMS.sha256 SHA: c3608dea0a7a372cdf11b5419157649758e2c476e96257e96d815ce07eb5e381
```

Verified result:

```text
status=PASS_PREBRANCH_ABORT_CLOSURE
branch_records=0
primary_window_steps=0
forced_open_steps=0
treatment_receipts=0
binary_consumable_label_count=0
v_phys_generated=false
protected counters=0
```

Interpretation: Parent 01 stopped before the first counterfactual branch and released its resources. Because the historical runtime set `outcomes_read_uncertain=true`, the conservative uncertainty marker must be preserved in all successor reports.

## 10. Root-cause classification and claim boundary

The confirmed defect is an authority/source mismatch between the frozen exact-plan selection plane and the formal runtime plane. The available evidence supports a structural exactness failure before the primary window. It does not yet prove a single mechanistic runtime cause for every differing field.

Do not claim that the M4 science failed, that a physical vulnerability was observed, or that a parent was repaired. The defensible claims are:

- Parent 00 is independently structurally invalidated before the primary window.
- Parent 01 is independently closed before any counterfactual branch.
- No consumable physical labels were generated by either closure path.
- Protected counters remain zero.
- The old formal authority is unusable under the current source-binding contract.

The following remain forbidden until a new authorization is sealed:

```text
formal M4 intervention outcomes
V_phys map
attack/VIS conclusions
Eval160 read
new Student or Teacher selection from M4 data
rerun-to-pass claims
```

## 11. Historical authority artifacts and supersession

The old M4 protocol and its runtime authorization are preserved as history. They must not be edited in place. Their historical fields are not evidence of current permission.

```text
current protocol V2 SHA: f21e8b689357e6269b0fe78c379d937a68c9bc0bcf3102fc085b29fabebc20a4
old static audit SHA: a13ceb910762e976d4d5ae6009ae50fba4b22c5d94f07d7824e5f323543e630b
old V2 runtime auth SHA: 20a16ed7a6916e6b407fe04c281ad34d4d8f7409c79b38c62d173f53d7561b7e
```

The old runtime authorization is historical/non-reusable after the source-plane guard and global hold. The next protocol must be a successor authority, not an edit to the historical JSON.

## 12. Required next work, in order

Only the following CPU/read-only or authority-preparation work is currently in scope.

### Step A: live re-audit

Re-read the GitHub PR head/tree/checks, local worktree, formal server worktree, formal process list, GPU ownership, and all closure roots. Treat this document as a handoff, not as a substitute for live verification.

### Step B: structured runtime diff

Build a read-only diff for the 96 Parent 00 branch records with one structured row per mismatched field:

```text
canonical_path
expected_type
actual_type
expected_shape
actual_shape
expected_sha256
actual_sha256
```

The diff must bind each row to parent, probe, branch, source commit/tree, runtime worktree, and closure report. It must not infer a mechanistic cause that the bytes do not establish.

### Step C: zero-treatment compatibility auditor

Implement the smallest separate auditor that is structurally unable to execute a treatment branch. It may:

- replay a frozen clean prefix;
- restore and compare simulator/runtime/primary-window bytes;
- close the environment;
- emit structured diffs and provenance.

It must not import or call `matched_action`, `_pair_label`, `v_phys_label`, `treatment_compliance`, or any M4 label producer. Add an AST/import boundary test or equivalent static proof that the auditor cannot reach those symbols.

### Step D: external runtime provenance

Record, independently of the formal runner:

```text
official Python path and version
source worktree commit/tree
snapshot and upstream roots
imported module hashes
model/checkpoint binding
diagnostic GPU identity, if a canary is later authorized
```

### Step E: conditional Q00 canary

Only after Steps A-D pass may the owner consider a zero-treatment, load/restore-only Q00 canary. It must use a fresh safe GPU ownership check and require `free_memory_mib > 20480`. It must not execute treatment, emit M4 labels, generate V_phys, or read protected data. If any gate fails, stop and preserve the evidence.

### Step F: 960/960 zero-treatment compatibility

Only after Q00 passes may the owner consider the same compatibility audit for all 40 x 24 frozen probes. Use the frozen IDs and steps exactly; never call probe selection again. Passing 960/960 is necessary but not sufficient for formal consumption.

### Step G: successor snapshot-rebind authority

Because the old exact-plan nested source fields are immutable and refer to a different plane, create a new append-only source/rebind receipt that maps the frozen exact plan to the audited successor runtime. The bridge must bind the exact plan SHA, snapshot bytes, runtime commit/tree, imported module hashes, Teacher/Student freeze SHA, pre-M4 lock SHA, firewall SHA, and compatibility-audit SHAs.

### Step H: independent authorization

Create a successor protocol and a separate runtime authorization receipt. The protocol should first be prospective/not authorized. The authorization may become executable only after independent static audit verifies:

- current final40 and split;
- exact 40 x 24 plan and zero duplicate/missing probes;
- primary firewall;
- Teacher/Student freeze and pre-M4 lock;
- checkpoint, feature-schema, and threshold SHAs;
- successor runtime commit/tree and runner/governance hashes;
- compatibility audit and snapshot rebind;
- all protected counters remain zero;
- no historical 29-parent or old-corridor assumption remains.

## 13. Explicit stop conditions

Stop and report for GPT/owner review if any of the following occurs:

- the exact plan must be regenerated or probes must be reseated;
- the runtime diff cannot distinguish pre-primary failure from post-primary execution;
- the compatibility auditor can import or call a treatment/label producer;
- any current source/tree/hash binding is incomplete or inconsistent;
- the official environment is unavailable;
- a GPU has a foreign owner or does not satisfy the strict memory threshold;
- any protected counter changes;
- any treatment receipt, binary label, or V_phys artifact appears during compatibility work;
- a requested fix would modify the historical Parent 00/01 roots, V1 protocol, V2 protocol, or frozen exact plan;
- any result would be selected because it looks favorable after the fact.

## 14. Out-of-scope actions

Do not perform any of the following in the receiving Goal Mode window without a fresh explicit owner authorization after all gates pass:

```text
do not launch formal M4
do not execute CONTROL/T3/T5/T10
do not create V_phys
do not launch attack or visual-robustness evaluation
do not read Eval160 or other protected evaluation
do not change Teacher labels or Student inputs
do not change final40/split
do not rerun Parent 00 or Parent 01 to obtain a favorable result
do not edit historical authorization artifacts in place
do not stop or attach to foreign GPU processes
```

## 15. Requested GPT review output

Please review this handoff against the current repository, PR #112, server evidence, and the authoritative machine-readable artifacts. Return:

1. A verdict on whether the current HOLD and both closure claims are correctly scoped.
2. Any P0/P1 authority, provenance, leakage, or claim-boundary defects.
3. A minimal ordered repair plan. Separate read-only audit work from any future formal runtime work.
4. Explicit acceptance criteria for the structured diff, zero-treatment auditor, 960/960 compatibility, snapshot rebind, successor protocol, and successor runtime authorization.
5. A new self-contained Goal Mode prompt for a fresh Codex window. The prompt must include:
   - exact repository and server roots;
   - the current branch/PR and required live re-verification;
   - the scientific ordering and Student contract;
   - immutable parent/closure evidence and hashes;
   - the exact allowed next actions;
   - the exact forbidden actions;
   - stop/HOLD conditions;
   - GitHub handoff/reporting requirements;
   - an instruction to pause for GPT review whenever an authority or scientific-interpretation choice is encountered.

The new prompt must not authorize formal M4 merely because a compatibility audit passes. It must require an independently audited successor authority and explicit owner approval before any held-out outcome read.

## 16. Handoff completion criterion

This document is complete when it is committed and pushed to PR #112, the final GitHub head/tree/checks are re-read, and a PR comment explicitly asks GPT to review this file and return the new Goal Mode prompt. Until that review and a new authority decision are received, the project remains in the current HOLD state.
