# Paper V1 evidence authority map

Status: `PAPER_V1_EVIDENCE_AUTHORITY_MAP_PASS`

The machine-readable authority map is the canonical P0 artifact. It records
source paths, immutable Git bindings, sealed-root hashes, denominators, claim
types, promotion status, and forbidden extrapolations for the mechanism-first
factorization-gap paper.

The primary unit remains the source-declared parent/episode. E3/E4 candidate
slots are diagnostic only and are never treated as iid scientific samples.
There are no identity-level joins across stages. Missing provenance remains
`NOT_IDENTIFIABLE`.

Stage VIII was absent from the current checkout but recovered from immutable
Git history and is bound by its blob hashes in the JSON map. Black Bowl is
retained only as bounded historical fixed-window mechanism context because its
raw sealed outcome denominator is not identifiable in this checkout.

Protected evaluation and Eval160 remain `UNREAD`; P0 added no runtime or GPU
activity.
